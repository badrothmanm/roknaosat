# 🚀 دليل إطلاق الموقع لعميل جديد (White-Label Onboarding)

مبروك على العميل الجديد! 🎉 بما أن المنصة أصبحت الآن نظاماً كخدمة (SaaS Template)، تكرار الموقع لأي شركة عقارية جديدة أصبح يستغرق دقائق بدلاً من أسابيع.

إليك الخطوات التفصيلية بالترتيب لتجهيز النسخة للعميل الجديد وتغيير هويته البصرية (الفرونت إند):

---

## الخطوة 1: نسخ خوادم المشروع (The Codebase)
1. قم بإنشاء مجلد جديد على حاسوبك باسم الشركة الجديدة (مثلاً: `alsafa_realestate`).
2. انسخ **كامل ملفات مشروع جودة المستقبل** إلى هذا المجلد الجديد (تأكد من عدم نسخ مجلد `venv` أو `db.sqlite3` لتبدأ بقاعدة بيانات نظيفة).

---

## الخطوة 2: تهيئة الهوية النصية (ملف `.env`)
افتح مجلد المشروع الجديد، وقم بنسخ ملف `.env.example` وأعد تسميته ليصبح `.env` فقط.
قم بتغيير القيم التالية لتطابق الشركة الجديدة:

```env
COMPANY_NAME="شركة الصفا"
COMPANY_NAME_FULL="شركة الصفا للتطوير والاستثمار العقاري"
COMPANY_PHONE="966500000000"
COMPANY_WHATSAPP="966500000000"
COMPANY_INSTAGRAM="alsafa.sa"
COMPANY_X_ACCOUNT="alsafa_sa"
```
✅ **ما الذي حدث هنا؟** بمجرد حفظ هذا الملف، كل النصوص في الموقع، حقوق الحفظ في الفوتر، رسائل الواتساب، وأسماء لوحة التحكم تغيرت تلقائياً!

---

## الخطوة 3: تهيئة الهوية البصرية (اللوجو والألوان)
بما أن كل عميل له ألوان وشعار مختلف في "الفرونت إند" (Front-end)، عليك تغيير ملفات بسيطة جداً:

### أ. تغيير الشعار (Logo):
اذهب إلى المسار التالي المشترك للصور:
`static/img/`
1. استبدل ملف `logo.png` بشعار العميل الجديد (يفضل أن يكون بخلفية شفافة).
2. استبدل ملف `favicon.ico` بأيقونة العميل الجديد (التي تظهر بجوار عنوان المتصفح).

### ب. تغيير ألوان الموقع (CSS Colors):
جميع ألوان موقع جودة (الكحلي الذهبي) مجمعة في أعلى ملفات الـ CSS لتسهيل تغييرها.
1. افتح ملف `static/css/style.css` (الخاص بواجهة الموقع).
2. في أعلى الملف ستجد متغيرات الألوان (`:root`). قم بتغييرها لألوان العميل:
```css
:root {
  /* للعميل الجديد، قم بتغيير هذا اللون إلى لونه الأساسي */
  --primary: #0d1b2e; /* مثلاً: أزرق الشركة الجديدة */
  
  /* اللون الثانوي (الذهبي سابقاً) */
  --gold: #c5a059; /* مثلاً: برتقالي الشركة الجديدة */
}
```
3. افتح ملف `static/css/admin_custom.css` (الخاص بلوحة التحكم).
4. استخدم ميزة (بحث واستبدال / Find and Replace) في محرر الأكواد لاستبدال لون جودة الكحلي `#0d1b2e` بلون العميل الجديد. واستبدال اللون الذهبي `#c5a059` باللون الثانوي للعميل.

✅ **النتيجة:** واجهة الموقع ولوحة التحكم أصبحت تتألق بألوان وشعار العميل الجديد تماماً!

---

## الخطوة 4: تهيئة الخدمات الخارجية (APIs)
في نفس ملف `.env` الخاص بالعميل الجديد، قم بإضافة مفاتيحه الخاصة حتى لا يختلط عمله بعمل جودة المستقبل:

1. **قاعدة البيانات:** أنشئ قاعدة بيانات جديدة على Render وضع بياناتها (DB_NAME, DB_USER...).
2. **Cloudinary (الصور):** افتح حساباً مجانياً للعميل في Cloudinary وضع مفاتيحه كي يتم حفظ صور عقاراته بشكل منفصل.
3. **Google Sheets (الطلبات):** انسخ سكريبت جوجل شيت المعتاد، أنشئ ملف إكسل بـ Drive العميل، وضع رابط الـ Webhook الجديد (`APPS_SCRIPT_URL`).

---

## الخطوة 5: رفع الموقع والتهيئة النهائية
1. ارفع المشروع المجهز إلى حسابك في `GitHub`.
2. في منصة `Render` (أو خادمك المفضل)، أنشئ Web Service جديد واربطه بـ GitHub.
3. انسخ محتويات ملف `.env` وضعها كـ **Environment Variables** في إعدادات Render.
4. بمجرد انتهاء الرفع، قم بإنشاء مدير أول للنظام عبر موجه الأوامر:
   `python manage.py createsuperuser`

🚀 **وأخيراً:** سلّم العميل لوحة التحكم الخاصة به، باسمه، وألوانه، ورابطه الخاص! جاهز للعمل.
